import type { ToolPageProps } from "@/types/tool";

/**
 * Copy this file when creating a new tool:
 *
 * 1. Duplicate this folder → tools/your-tool-slug/
 * 2. Rename the component
 * 3. Register in lib/tools/registry.ts (toolRegistrations array)
 */
export default function StarterTool(_props: ToolPageProps) {
  return null;
}
